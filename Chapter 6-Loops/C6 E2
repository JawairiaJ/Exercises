prompt = "Enter your age: "
prompt += "\nEnter 'quit' if you are finished. "

while True:
    age = input(prompt)
    if age == 'quit':
        break
    age = int(age)
    
    if age < 3: print ("Free ticket.")
    elif age < 13 : print ("Ticket: $10")
    else: print ("Ticket: $15")